import SwiftUI

struct ForgotPasswordView: View {
    var body: some View {
        Text("準備中です")
            .font(.title)
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}

#Preview {
    ForgotPasswordView()
}
